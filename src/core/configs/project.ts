const project = Object.freeze({
  APP_NAME: '@example_',
  APP_DEFAULT_LANG: 'en' as I18n['language'],
  APP_DEFAULT_THEME: 'light' as ITheme['type'],
  APP_LANGS: ['en', 'pl'] as I18n['language'][],
});

export default project;
